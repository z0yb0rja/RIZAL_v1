 VALID8 System Components: Meanings and Functions
Generated: 2026-03-06

Format:
- Component
  Meaning:
  Function in VALID8:

========================================================================
CORE PLATFORM AND INFRASTRUCTURE
========================================================================

- Docker Compose
  Meaning: Multi-container orchestration for local/server deployment.
  Function in VALID8: Runs backend API, Celery worker, PostgreSQL, Redis, frontend, and pgAdmin together.

- Backend Service (FastAPI container)
  Meaning: Main API server process.
  Function in VALID8: Handles auth, users, events, attendance, audit logs, notifications, import APIs, security center, subscription, and governance endpoints.

- Worker Service (Celery container)
  Meaning: Background task processor.
  Function in VALID8: Executes long-running jobs like bulk student import processing and welcome email delivery retries.

- PostgreSQL (db)
  Meaning: Relational database.
  Function in VALID8: Stores all application data (users, schools, events, attendance, imports, audit logs, notifications, security/session data, governance data).

- Redis
  Meaning: In-memory data store / message broker.
  Function in VALID8: Broker/result backend for Celery async jobs.

- pgAdmin
  Meaning: Web-based PostgreSQL administration tool.
  Function in VALID8: Lets admins/devs inspect tables, run SQL, and manage DB objects.

========================================================================
BACKEND FRAMEWORK AND DATA LAYER
========================================================================

- FastAPI
  Meaning: Python web framework for APIs.
  Function in VALID8: Provides REST endpoints, request validation, dependency injection, and OpenAPI docs.

- Uvicorn
  Meaning: ASGI server for Python async apps.
  Function in VALID8: Runs the FastAPI application.

- SQLAlchemy
  Meaning: Python ORM and SQL toolkit.
  Function in VALID8: Defines models and executes DB queries/transactions.

- Alembic
  Meaning: Database migration tool for SQLAlchemy.
  Function in VALID8: Applies schema changes (new tables, columns, indexes, enum fixes).

- Pydantic
  Meaning: Data validation and schema library.
  Function in VALID8: Validates API request/response models and enforces typed schemas.

========================================================================
AUTHENTICATION, AUTHORIZATION, AND SECURITY
========================================================================

- JWT (JSON Web Token)
  Meaning: Signed token-based authentication format.
  Function in VALID8: Encodes user identity, roles, school scope, and token/session IDs for API auth.

- OAuth2 Bearer Token Flow
  Meaning: Standard token auth pattern for APIs.
  Function in VALID8: Protects private routes and requires valid Bearer tokens for access.

- bcrypt + passlib
  Meaning: Password hashing algorithms/utilities.
  Function in VALID8: Hashes and verifies user passwords securely.

- MFA (Multi-Factor Authentication)
  Meaning: Additional verification step beyond password.
  Function in VALID8: Admin/School IT login can require a one-time code (email-delivered) before token issuance.

- MFA Challenge Store (`mfa_challenges`)
  Meaning: Temporary challenge records for MFA attempts.
  Function in VALID8: Tracks code hash, attempts, expiry, and consumption status.

- Session Management (`user_sessions`)
  Meaning: Server-side token session tracking.
  Function in VALID8: Creates/revokes sessions, supports "revoke other sessions", and validates token session state.

- Login History (`login_history`)
  Meaning: Authentication activity log.
  Function in VALID8: Stores success/failure, auth method, IP, and user-agent for security monitoring.

- Role-Based Access Control (RBAC)
  Meaning: Permission model by user role.
  Function in VALID8: Restricts endpoint access for roles like admin, school_IT, student, ssg, event-organizer.

- School Scope Enforcement
  Meaning: Tenant isolation by school.
  Function in VALID8: Prevents cross-school data access for most role actions.

- CORS Middleware
  Meaning: Cross-Origin Resource Sharing policy.
  Function in VALID8: Allows frontend origin(s) (e.g., localhost:5173) to call backend APIs.

========================================================================
COMMUNICATION AND NOTIFICATIONS
========================================================================

- SMTP
  Meaning: Simple Mail Transfer Protocol for sending emails.
  Function in VALID8: Sends MFA codes, welcome emails, password reset emails, and security/notification emails.

- Email Service (`email_service.py`)
  Meaning: Central outbound email module.
  Function in VALID8: Abstracts SMTP send logic and standard email templates.

- Notification Center
  Meaning: Central notification orchestration module.
  Function in VALID8: Sends and logs notifications for test alerts, missed events, low attendance, account security, and subscription reminders.

- Notification Preferences (`user_notification_preferences`)
  Meaning: Per-user notification settings.
  Function in VALID8: Controls channels (email/SMS) and category-level opt-in/opt-out.

- Notification Logs (`notification_logs`)
  Meaning: Delivery records for notifications.
  Function in VALID8: Tracks category, channel, status, payload metadata, and failures.

- SMS Channel (Current Status)
  Meaning: Text-message notification channel.
  Function in VALID8: Placeholder only; currently logs failed/skipped when SMS provider is not configured.

========================================================================
SCHOOL OPERATIONS, AUDIT, AND ADMIN MODULES
========================================================================

- Schools (`schools`)
  Meaning: School tenant master table.
  Function in VALID8: Stores school identity, branding basics, and subscription linkage.

- School Settings (`school_settings`)
  Meaning: School-specific configuration.
  Function in VALID8: Stores school customization values and operational settings.

- School Audit Logs (`school_audit_logs`)
  Meaning: School-scoped action trail.
  Function in VALID8: Captures admin/IT actions and system operation outcomes for traceability.

- Audit Logs UI/API
  Meaning: Searchable audit interface.
  Function in VALID8: Lets Admin/School IT query actions by text, actor, status, date, and school scope.

========================================================================
IMPORT CENTER
========================================================================

- Bulk Import Jobs (`bulk_import_jobs`)
  Meaning: Import execution records.
  Function in VALID8: Tracks file imports, state, progress, success/fail counts, and reports.

- Bulk Import Errors (`bulk_import_errors`)
  Meaning: Row-level import error records.
  Function in VALID8: Stores invalid row number/error details for review and retry.

- Import Validation Service
  Meaning: Validation and transformation logic for Excel rows.
  Function in VALID8: Checks headers, required fields, duplicates, email format, department/course existence, and provides fix suggestions.

- Import Preview-before-Commit
  Meaning: Pre-queue validation endpoint.
  Function in VALID8: Shows valid/invalid rows and suggested fixes before import commit.

- Retry Failed Rows
  Meaning: Recovery workflow for partial failures.
  Function in VALID8: Rebuilds a retry file from failed rows and queues a new import job.

- OpenPyXL
  Meaning: Python Excel processing library.
  Function in VALID8: Reads/writes .xlsx files for import templates, previewing, and failed-row reports.

- Email Delivery Logs (`email_delivery_logs`)
  Meaning: Outbound import-related email delivery tracking.
  Function in VALID8: Stores send status/retries for welcome emails sent from worker tasks.

========================================================================
ACCOUNT MANAGEMENT WORKFLOWS
========================================================================

- Password Reset Request Workflow (`password_reset_requests`)
  Meaning: Approval-based reset mechanism.
  Function in VALID8: Student reset requests are reviewed by School IT/Admin, then temporary password is emailed.

- Forced Password Change (`must_change_password`)
  Meaning: Security flag after account setup/reset.
  Function in VALID8: Requires password change before accessing protected app resources.

========================================================================
SUBSCRIPTION AND GOVERNANCE MODULES
========================================================================

- School Subscription Settings (`school_subscription_settings`)
  Meaning: School plan and usage limit config.
  Function in VALID8: Stores plan, user/event/import limits, renewal date, and reminder lead time.

- School Subscription Reminders (`school_subscription_reminders`)
  Meaning: Renewal reminder records.
  Function in VALID8: Tracks reminder creation/sent/failed status for renewals.

- Data Governance Settings (`data_governance_settings`)
  Meaning: School-level retention and governance policy config.
  Function in VALID8: Controls retention days and auto-delete behavior.

- User Privacy Consents (`user_privacy_consents`)
  Meaning: Consent history records.
  Function in VALID8: Tracks user consent type, version, source, and grant status.

- Data Requests (`data_requests`)
  Meaning: User data export/delete workflow records.
  Function in VALID8: Manages request lifecycle (pending/approved/rejected/completed), outputs, and handlers.

- Data Retention Run Logs (`data_retention_run_logs`)
  Meaning: Cleanup execution logs.
  Function in VALID8: Records dry-run/live retention cleanup summaries and status.

========================================================================
ATTENDANCE AND FACE RECOGNITION
========================================================================

- Attendance Module (`attendances`)
  Meaning: Event attendance records.
  Function in VALID8: Stores time-in/out, method, status, verifier, and notes.

- Face Recognition Service
  Meaning: Biometric face matching module.
  Function in VALID8: Registers and recognizes student face encodings for attendance workflows.

- face_recognition + dlib
  Meaning: Python face detection/encoding libraries.
  Function in VALID8: Provides underlying model operations for face registration and matching.

========================================================================
FRONTEND STACK
========================================================================

- React
  Meaning: Component-based frontend library.
  Function in VALID8: Builds dashboards/pages for Admin, School IT, Student, SSG, and event organizers.

- Vite
  Meaning: Frontend dev server and build tool.
  Function in VALID8: Runs local frontend dev server and creates production bundles.

- React Router
  Meaning: Client-side routing library.
  Function in VALID8: Handles role-based page routing/navigation.

- Axios/Fetch API Layer
  Meaning: HTTP client integration.
  Function in VALID8: Calls backend APIs for auth, users, events, imports, notifications, security, subscription, and governance.

- Chart Libraries (Chart.js / Recharts)
  Meaning: Data visualization libraries.
  Function in VALID8: Renders attendance and dashboard charts/reports.

========================================================================
CURRENT PRACTICAL NOTES
========================================================================

- SMTP is active for email flows; ensure valid provider credentials in env vars for production.
- SMS dispatch path exists but provider integration is still placeholder.
- Current compose file runs frontend via `npm run dev` and backend via `uvicorn --reload` (development mode).
