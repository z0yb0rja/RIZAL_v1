VALID8 Website Changes Summary (Current Working Tree)
Generated: 2026-03-06

Scope note
- This report reflects uncommitted work currently present in this repository.

Implemented feature groups
1) Multi-school foundation
- School-scoped architecture (users/events/attendance and role access).
- School IT flows and school-level settings/branding.

2) Notification system
- User preferences for email/SMS + categories.
- Notification logs and dispatch services.
- Trigger endpoints for missed events, low attendance, and account/security actions.

3) Audit log UI (Admin + School IT)
- Searchable audit logs page connected to school_audit_logs.
- Filtering and scoped visibility by role/school.

4) Import center improvements
- Preview-before-commit for student import.
- Per-row validation feedback with fix suggestions.
- Retry only failed rows for a prior import job.

5) Stronger account security
- MFA settings and challenge verification flow.
- Session tracking/revocation endpoints.
- Login history logging and security notifications.

6) School subscription/admin module
- Plan/limits model (users/events/imports).
- Usage metrics endpoints.
- Renewal reminder records and dispatch support.

7) Data governance tools
- Retention settings per school.
- Export/delete request workflow with statuses.
- Privacy consent tracking and retention run logs.

Backend additions (high level)
- New routers: audit_logs, notifications, security_center, subscription, governance.
- Auth updates: MFA-required login flow, MFA verification endpoint, session-aware JWT checks.
- New services: notification_center_service, security_service, import_validation enhancements.
- New schemas for audit/notification/security/subscription/governance.
- New migration: c3d4e5f6a7b8_add_platform_ops_security_notifications_governance.py.

Frontend additions (high level)
- New pages: AuditLogs, NotificationCenter, SecurityCenter, SubscriptionCenter, DataGovernance.
- LoginForm MFA step and auth API MFA handling.
- Import page enhancements for preview and retry-failed.
- New Admin + School IT navigation links and routes.

Current containers
- backend, worker, frontend, db, pgadmin, redis are running via docker compose.
